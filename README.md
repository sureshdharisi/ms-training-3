Execution Details for spring-aws-integration:

1. Setting up the axon server:
   Download the Axon server as a jar file
   Execute the below command to start the Axon server:
   ```
   java -jar axonserver.jar
   ```
